{
  "name": "envvar-to-dotenv-action",
  "version": "0.1.0",
  "description": "A GitHub action that appends environment variables to a dotenv file",
  "main": "dist/index.js",
  "license": "GPL-3.0-only",
  "scripts": {
    "build": "npx ncc build src/main.ts -o dist",
    "test": "npx jest"
  },
  "dependencies": {
    "@actions/core": "^1.5.0",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "@types/node": "^16.9.6",
    "@vercel/ncc": "^0.31.1",
    "jest": "^27.2.1",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.3"
  }
}
